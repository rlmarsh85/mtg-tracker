{% extends 'base.html.twig' %}

{% block title %}GamePlayer{% endblock %}

{% block body %}
    <h1>GamePlayer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ game_player.id }}</td>
            </tr>
            <tr>
                <th>WinningPlayer</th>
                <td>{{ game_player.WinningPlayer }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('game_player_index') }}">back to list</a>

    <a href="{{ path('game_player_edit', {'id': game_player.id}) }}">edit</a>

    {{ include('game_player/_delete_form.html.twig') }}
{% endblock %}
