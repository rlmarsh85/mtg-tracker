{{ form_start(form) }}


    {{ form_label(form.Name) }}
    {{ form_widget(form.Name) }}

    {{ form_label(form.Scryfall_URL) }}
    {{ form_widget(form.Scryfall_URL) }}    
         
    {{ form_label(form.colors) }}
    {% for field in form.colors %}
       <div>
         {{ form_widget(field) }}
         {{ form_label(field) }}
       </div>
    {% endfor %}


    <div class = "row">
        <div class = "col-6">
            <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
        </div>
{{ form_end(form) }}

        {% if is_update|default(false) == true %}
        <div class = "col-6 delete-btn">
             {{ include('commander/_delete_form.html.twig') }}
        </div>
        {% endif %}

    </div>
