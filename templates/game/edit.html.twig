{% extends 'base.html.twig' %}

{% block title %}Edit Game{% endblock %}

{% block body %}
    <div class = "container">
        <h1>Edit Game</h1>

        <div class = "row">
            <div class = "col-11">
                {{ include('game/_form.html.twig', {'button_label': 'Update', 'is_update': true}) }}
            </div>
        </div>

        <a href="{{ path('game_index') }}"> <- Back to List</a>
        
    </div>

{% endblock %}
